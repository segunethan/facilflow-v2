{
  "name": "facilflow-v2",
  "version": "2.0.0",
  "description": "Africa Prudential FaciliFlow — Monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"USER,ADMIN\" --prefix-colors \"red,blue\" \"npm run dev --prefix facilflow-user\" \"npm run dev --prefix facilflow-admin\"",
    "dev:user": "npm run dev --prefix facilflow-user",
    "dev:admin": "npm run dev --prefix facilflow-admin",
    "install:all": "npm install --prefix facilflow-user && npm install --prefix facilflow-admin",
    "build:user": "npm run build --prefix facilflow-user",
    "build:admin": "npm run build --prefix facilflow-admin",
    "build": "npm run build:user && npm run build:admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}